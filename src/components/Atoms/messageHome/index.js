const HomeMessage = () => {
  let component;

  return (
    <div className="constainer-fluid bg-succes " style={{ height: 215 }}>

      {component}
    </div>
  );
};

export default HomeMessage;
